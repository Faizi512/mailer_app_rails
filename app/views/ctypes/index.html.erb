
<div class="container" id="containerb">

<h3> All Photography Categories </h3>

<table class="table table-condensed">

  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ctypes.each do |ctype| %>
      <tr>
        <td><%= ctype.name %></td>
        <td><%= link_to 'Show', ctype %></td>
        <td><%= link_to 'Edit', edit_ctype_path(ctype) %></td>
        <td><%= link_to 'Destroy', ctype, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New type of Category', new_ctype_path %>

</div>