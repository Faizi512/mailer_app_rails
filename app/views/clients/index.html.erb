
<div class="container" id="containerb">

<p class="attn">Client Index (<%= @clients.count %>)</p> <br>




<table class="table table-hover table-condensed">
<thead> 
    <tr>
      <th>Client name</th>
      <th># of Projects</th>
      <th>Email address</th>
      <th>Phone Number</th>
      <th>Admin Shortcut</th>
    
    </tr>
  </thead>


    <tbody> 
     <% @clients.each do |client| %>
    <tr>
    <td><%= link_to client.name, client_path(client) %></td>
    <td><%= client.projects.count %></td>
    <td><%= client.email %></td>
    <td><%= client.phonenumber %></td>

    <td>  <% if admin_signed_in? && !client_signed_in? %>
     <%= link_to "delete", client, method: :delete, data: { confirm: "Are you sure? This action cannot be undone." }, class: "linkdanger" %></td>

  <% end %>
    </tr>
    </tbody>
  
  <% end %>
</table>

<%= will_paginate %>

</div>