<div class="container" id="containerb">

<h3>Client Index (<%= @clients.count %>)</h3> <br>



<table class="table">

    <tbody> 
    	  <% @clients.each do |client| %>
    	<tr>
    		<td><%= link_to client.name, client_path(client) %></td>

    	</tr>
    </tbody>
  
  <% end %>
</table>

<%= will_paginate %>

</div>